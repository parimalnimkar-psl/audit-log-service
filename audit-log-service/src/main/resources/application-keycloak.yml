spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/audit}
  h2:
    console:
      enabled: false

app:
  keycloak:
    issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/audit}