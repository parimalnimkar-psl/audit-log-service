spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/audit_log_db
    username: ${DB_USERNAME:audit_user}
    password: ${DB_PASSWORD:change_me}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
