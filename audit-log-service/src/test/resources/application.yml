management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

app:
  jwt:
    secret: test-only-secret-key-that-is-at-least-32-characters
    issuer: audit-log-service
    expiration-minutes: 120
  audit:
    genesis-hash: GENESIS
    retention-days: 365
